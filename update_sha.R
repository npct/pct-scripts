# Update the data sha
old = setwd("../pct-data/")
mes_add = paste0("git add ", region, "/*")
system(mes_add)
mes_commit = paste0("git commit -m 'Add new data for ", region, "'")
system(mes_commit)
system("git push origin master")
(newsha = system("git rev-parse --short HEAD", intern = T))
setwd("../pct-shiny/")
writeLines(newsha, "data_sha")
system(paste0("git commit -am 'Update data for ", region, "'"))
system("git push origin master")
setwd(old)
